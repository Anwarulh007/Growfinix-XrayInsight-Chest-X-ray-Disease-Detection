# Chest X-ray Detection + Grad-CAM

## Quick start

1. Put your images in `data/images/` (names like `00000001_000.png`) and CSV at `data/labels.csv`.
2. Create virtualenv and install requirements:
